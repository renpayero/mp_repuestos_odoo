# Dependencias Python para Odoo 19 CE + facturación electrónica ARCA (ADHOC).
# Las 5 primeras provienen de ingadhoc/odoo-argentina-ce@19.0/requirements.txt.
# M2Crypto compila contra OpenSSL → ver deps de sistema en el Dockerfile.
pyOpenSSL
M2Crypto
httplib2>=0.7
pysimplesoap~=1.8.22
# pyafipws pineado a commit fijo (no a la rama) para evitar cambios silenciosos del WSDL/API
# de ARCA al rebuildar la imagen. Rama py3k del fork filoquin (compatible con Python 3.12).
git+https://github.com/filoquin/pyafipws.git@0bb5988b31f066ca1906156183a3a0888643c314
# report_xlsx (OCA/reporting-engine), dependencia de l10n_ar_reports (Libro IVA):
xlrd
xlsxwriter
